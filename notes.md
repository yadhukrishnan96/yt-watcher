# Currently working on
 - [x] need to create /metric in app
    - uncommited changes






# Installing entire prometheus stack with helm for obsservability
 - stack installed
  - something 




# for secrets mngt
    SOPS + age
    keys stored in -n ~/.config/sops/age/keys.txt

implemented docker buildx


e42aa2a






curl http://yt.local/oom to initiate oom kill


added /readyz endpoint for pgres and redis availability



# Paths you can hit for eg
 - curl http://localhost:8000/healthz


GET    /healthz
GET    /docs
GET    /redoc
GET    /openapi.json
GET    /api/v1/status
GET    /api/v1/videos
GET    /api/v1/videos?limit=50
GET    /api/v1/videos?channel_id=<CHANNEL_ID>
GET    /api/v1/videos?channel_id=<CHANNEL_ID>&limit=<N>

POST   /api/v1/poll

DELETE /api/v1/videos/<VIDEO_ID>





